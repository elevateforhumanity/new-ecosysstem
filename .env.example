# API base (optional). Example:
# VITE_API_BASE_URL=https://api.example.com

# --- Google Configuration ---
# Google Analytics 4 Measurement ID (e.g., G-XXXXXXXXXX)
VITE_GA_MEASUREMENT_ID=GA_MEASUREMENT_ID
# Google Search Console verification code
GOOGLE_SITE_VERIFICATION=GOOGLE_VERIFICATION_CODE_HERE
# Canonical domain for sitemaps and SEO
CANONICAL_DOMAIN=https://example.com

# --- Server Runtime ---
NODE_ENV=development
LOG_LEVEL=info
JWT_SECRET=change_me_to_a_secure_random_value

# --- Database (optional for now) ---
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# --- Stripe (optional; enables real payment intent creation) ---
# STRIPE_SECRET_KEY=sk_live_xxx

# --- Supabase Configuration ---
# Supabase URL (public)
VITE_SUPABASE_URL=https://your-project.supabase.co
# Supabase anonymous key (public)
VITE_SUPABASE_ANON_KEY=your-anon-key
# Supabase URL for server (same as VITE_SUPABASE_URL)
SUPABASE_URL=https://your-project.supabase.co
# Supabase service role key (server, secret)
SUPABASE_SERVICE_KEY=your-service-role-key
