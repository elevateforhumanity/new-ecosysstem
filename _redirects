# Pass static assets straight through
/assets/*  /assets/:splat  200

# TEMPORARILY DISABLED: Redirect causing loop with external redirect rule
# https://elevateforhumanity.org/* https://www.elevateforhumanity.org/:splat 301!
# 
# ISSUE: External service (Cloudflare/Netlify) is redirecting www→non-www
# This conflicts with our non-www→www redirect, creating infinite loop

# Note: HTTPS redirect removed - Cloudflare handles HTTP->HTTPS automatically
# Keeping this would create a redirect loop with Cloudflare's "Always Use HTTPS"

# Core ecosystem pages (serve actual HTML files)
/hub /hub.html 200
/programs /programs.html 200
/lms /lms.html 200
/connect /connect.html 200
/compliance /compliance.html 200
/partners /partners.html 200

# Student portal clean URLs
/student-portal /student-portal.html 200
/enroll /student-portal.html 200
/login /student-portal.html 200
/certificates /student-portal.html 200

# SPA fallback ONLY for unmatched routes
/*          /index.html     200