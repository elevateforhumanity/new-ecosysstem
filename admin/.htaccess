# Deny all access to this directory
Order deny,allow
Deny from all

# Prevent directory listing
Options -Indexes

# Prevent access to all files
<Files "*">
    Order deny,allow
    Deny from all
</Files>