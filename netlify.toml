[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18.20.4"          # pin Node 18 LTS
  NPM_FLAGS = "--legacy-peer-deps"  # relax peer deps on Netlify

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200