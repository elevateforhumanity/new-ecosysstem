[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  SITE_URL = "https://your-site.netlify.app" # override in Netlify UI

# React Router / SPA fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200